
# Usually we would have a config yaml 
# than then is read into a Python Config class
# for this example, we skip the first step and leave it here.
class Config:

    MODEL_PATH = "models/online_model_1.pkl"
    DATA_INPUT = "data/queries/"
    FAKE_USER_TST = "data/fake_data/fake_input.pkl"